/**
 * Data stored in Zustand for IFC state.
 *
 * @param {Function} set
 * @param {Function} get
 * @return {object} API slice.
 */
export default function createAPISlice(set, get) {
  return {
    uri: null,
  }
}
