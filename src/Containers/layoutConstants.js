/**
 * Cross-container layout constants.
 *
 * These are shared because two containers have to agree on them: the
 * ProjectsDrawer header row aligns with the top bar over the canvas, so
 * a change in one that isn't mirrored in the other is immediately
 * visible as a misalignment.
 */


/** Height of the top bar over the canvas, and of the drawer header row */
export const TOP_BAR_HEIGHT = 58
