# dependencies
/node_modules

# no longer check-in docs for builds.  This is handled by Netlify
/docs

cypress/downloads
cypress/e2e/logs

# playwright
tools/playwright-report
tools/test-results
test-results
.cache

/coverage

# symlink for test-models repo
public/test-models

# misc
.DS_Store
.vscode
.idea
*.log
*~
\#*
.\#*

# Certificates
*.pem

# Local Netlify folder
.netlify

# netlify-cli writes deno.lock for its edge-functions toolchain
deno.lock

# Claude Code local settings
.claude/settings.local.json
.claude/scheduled_tasks.lock

# SessionStart hook per-step completion stamps (runtime state)
.claude/.bootstrap/

# Yarn Berry (nodeLinker: node-modules). Commit the release binary; ignore the
# fetch cache and per-install state. PnP is not used.
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
.yarn/install-state.gz
